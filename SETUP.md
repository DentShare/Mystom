# MiniStom Bot - Документация по установке и запуску

## Проделанная работа

### ✅ Реализованные компоненты

#### 1. Структура проекта
- Создана полная структура папок согласно плану
- Настроены все необходимые конфигурационные файлы
- Создан `.gitignore` для исключения ненужных файлов

#### 2. База данных (PostgreSQL + SQLAlchemy Async)
- **Модели:**
  - `User` (Doctor) - информация о врачах с уровнями подписки
  - `ClinicLocation` - мульти-локации клиник
  - `Patient` - база пациентов (Standard+)
  - `Appointment` - записи на прием
  - `Treatment` - история лечения с финансовыми данными (Premium)
  - `Service` - прайс-лист услуг (Premium)
  - `ImplantLog` - имплантологическая карта (Standard+)
- Настроено подключение к PostgreSQL через asyncpg
- Подготовлена конфигурация Alembic для миграций

#### 3. Middleware
- **UserMiddleware** - автоматическое получение пользователя из БД
- **SubscriptionMiddleware** - проверка уровня подписки через флаги хендлеров
  - Блокирует доступ к функциям, если уровень подписки недостаточен
  - Показывает понятные сообщения об ошибках

#### 4. Регистрация и авторизация
- FSM для регистрации врача:
  - Ввод ФИО, специализации, телефона, адреса
  - Загрузка геолокации и фото
  - Выбор часового пояса
- Автоматическое создание пользователя при первом запуске

#### 5. Главное меню
- Динамические клавиатуры в зависимости от уровня подписки:
  - **Basic (tier=0)**: Календарь, Визитка
  - **Standard (tier=1)**: + Пациенты, История болезни, Имплантологическая карта
  - **Premium (tier=2)**: + Финансы, Прайс-лист, Экспорт

#### 6. Визитка врача
- Генерация красивого сообщения с данными врача
- Кнопка для открытия геолокации на карте
- Команда `/visiting_card`

#### 7. Календарь и записи (Basic+)
- Inline-календарь с навигацией по месяцам
- Выбор времени через слоты (30/60 минут)
- Запись пациентов:
  - **Basic**: текстовый ввод "ФИО - Услуга"
  - **Standard+**: выбор/создание пациента из базы
- Команды `/today` и `/schedule [дата]` для просмотра расписания
- Отмена и перенос визитов (Standard+)

#### 8. Управление пациентами (Standard+)
- Создание, редактирование, просмотр пациентов
- Умный поиск по ФИО или телефону (ILIKE)
- CRUD операции
- Просмотр профиля пациента

#### 9. История болезни (Standard+)
- Добавление текстовых заметок к пациентам
- Просмотр истории с датами
- Интеграция с профилем пациента

#### 10. Имплантологическая карта (Standard+)
- **FSM для добавления импланта:**
  - Выбор зуба через зубную формулу (4 квадранта) или ручной ввод
  - Выбор системы импланта (популярные бренды или своя)
  - Выбор размера (популярные размеры или ручной ввод)
  - Опциональные заметки
- **Генерация PDF карты имплантации:**
  - HTML-шаблон с медицинским стилем
  - Таблица со всеми имплантами пациента
  - Данные врача и пациента
  - Генерация через WeasyPrint
- Интеграция в профиль пациента

#### 11. Финансовый модуль (Premium)
- Базовый модуль для работы с финансами
- Статистика по записям и суммам
- Интеграция с Treatment для учета оплат

#### 12. Админ-панель
- `/admin` — меню админа (справка и кнопка Web App)
- `/admin_set_tier <user_id> <tier>` — установка уровня подписки
- `/admin_set_subscription <user_id> <tier> <days>` — подписка на N дней с датой окончания
- `/admin_list_users` — список пользователей (уровень, срок подписки)
- **Web App** — веб-админка: пользователи, уровни, сроки, ручное управление (см. раздел ниже)

#### 13. Генерация PDF
- **Карта имплантации** - для Standard+
- **Счет** - шаблон для Premium (готов к использованию)
- Использование Jinja2 для шаблонов
- WeasyPrint для генерации PDF

#### 14. Утилиты
- Валидаторы для телефонов, дат, номеров зубов, цен
- Форматтеры для дат, денежных сумм, имен пациентов

#### 15. Main.py
- Инициализация бота и диспетчера
- Регистрация всех middleware и роутеров
- Запуск polling
- Обработка ошибок и закрытие соединений

---

## Инструкция по запуску

### Шаг 1: Установка зависимостей

```bash
# Создайте виртуальное окружение (рекомендуется)
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 2: Настройка базы данных

1. Установите PostgreSQL (если еще не установлен)
2. Создайте базу данных:
```sql
CREATE DATABASE ministom;
```

3. Создайте пользователя (опционально):
```sql
CREATE USER ministom_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE ministom TO ministom_user;
```

### Шаг 3: Настройка переменных окружения

1. Создайте файл `.env` в корне проекта:
```bash
# Windows PowerShell
Copy-Item .env.example .env

# Linux/Mac
cp .env.example .env
```

2. Отредактируйте `.env` и заполните значения:
```env
BOT_TOKEN=your_telegram_bot_token_here
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/ministom
ADMIN_IDS=123456789,987654321
TIMEZONE_API_KEY=optional_for_auto_detect

# Покупка подписки (кнопка «Подписка» в боте)
PAYMENT_CARD=8600********1234
PAYMENT_RECEIVER=Иванов И. И.
ADMIN_CONTACT=@admin_username
SUBSCRIPTION_STANDARD_PRICE=99 000 сум/мес
SUBSCRIPTION_PREMIUM_PRICE=199 000 сум/мес
```

**Где взять BOT_TOKEN:**
1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot` и следуйте инструкциям
4. Скопируйте полученный токен в `.env`

**ADMIN_IDS:**
- Это ваш Telegram ID (можно узнать у [@userinfobot](https://t.me/userinfobot))
- Можно указать несколько ID через запятую

**Покупка подписки (опционально):** если заполнены `PAYMENT_CARD`, `PAYMENT_RECEIVER`, `ADMIN_CONTACT`, в боте по кнопке «⭐ Подписка» пользователи увидят реквизиты для перевода и контакт администратора. После оплаты администратор активирует подписку командами `/admin_set_subscription` или через админ-панель. В перспективе планируется интеграция с **Uzum Click Pay** для автоматической оплаты в боте.

### Шаг 4: Применение миграций базы данных

```bash
# Создайте первую миграцию (если еще не создана)
alembic revision --autogenerate -m "Initial migration"

# Примените миграции
alembic upgrade head
```

**Примечание:** Если команда `alembic` не найдена, убедитесь, что виртуальное окружение активировано и зависимости установлены.

### Шаг 5: Запуск бота

```bash
# Из корневой директории проекта
python -m app.main
```

Или:

```bash
python app/main.py
```

### Шаг 6: Проверка работы

1. Откройте Telegram
2. Найдите вашего бота по имени, которое вы указали в BotFather
3. Отправьте команду `/start`
4. Пройдите регистрацию
5. Используйте `/menu` для доступа к главному меню

---

## Управление подписками (для администраторов)

После запуска бота вы можете управлять подписками пользователей:

```bash
# Установить уровень подписки
/admin_set_tier <telegram_id> <tier>
# tier: 0 = Basic, 1 = Standard, 2 = Premium

# Установить подписку на определенный срок
/admin_set_subscription <telegram_id> <tier> <days>

# Просмотреть список пользователей
/admin_list_users
```

**Пример:**
```
/admin_set_tier 123456789 1
/admin_set_subscription 123456789 2 30
```

---

## Структура проекта

```
MiniStom/
├── app/
│   ├── main.py                 # Точка входа
│   ├── config.py               # Конфигурация
│   ├── database/               # Модели и работа с БД
│   │   ├── models.py           # Все модели SQLAlchemy
│   │   ├── base.py             # Подключение к БД
│   │   └── session.py           # Сессии
│   ├── middleware/             # Middleware
│   │   ├── user.py             # UserMiddleware
│   │   └── subscription.py     # SubscriptionMiddleware
│   ├── handlers/               # Обработчики команд
│   │   ├── start.py            # Регистрация
│   │   ├── menu.py             # Главное меню
│   │   ├── business_card.py    # Визитка
│   │   ├── calendar.py         # Календарь и записи
│   │   ├── patients.py         # Управление пациентами
│   │   ├── history.py           # История болезни
│   │   ├── implant.py           # Имплантологическая карта
│   │   ├── finance.py           # Финансы
│   │   └── admin.py             # Админ-панель
│   ├── keyboards/              # Клавиатуры
│   │   ├── main.py             # Главное меню
│   │   ├── calendar.py          # Календарь
│   │   └── implant.py           # Импланты
│   ├── services/               # Бизнес-логика
│   │   ├── calendar_service.py # Логика календаря
│   │   ├── patient_service.py  # Работа с пациентами
│   │   ├── pdf_generator.py    # Генерация PDF
│   │   └── timezone.py         # Часовые пояса
│   ├── states/                 # FSM состояния
│   │   ├── registration.py     # Регистрация
│   │   ├── appointment.py      # Запись на прием
│   │   ├── patient.py          # Пациенты
│   │   └── implant.py          # Импланты
│   └── utils/                  # Утилиты
│       ├── validators.py        # Валидация
│       └── formatters.py        # Форматирование
├── templates/                   # HTML шаблоны для PDF
│   ├── implant_card.html       # Карта имплантации
│   └── invoice.html            # Счет
├── static/logos/                # Логотипы врачей
├── alembic/                     # Миграции БД
├── requirements.txt             # Зависимости
├── .env.example                 # Пример .env
├── README.md                    # Основная документация
└── SETUP.md                     # Этот файл
```

---

## Уровни подписки

В боте доступны **команда /faq** и кнопка **«❓ Подробно о возможностях (FAQ)»** в разделе «⭐ Подписка» — там пользователи видят развёрнутое описание тарифов и ответы на вопросы «за что платить» и «почему удобно работать».

### Basic (tier=0) — Базовый (бесплатный старт)
- ✅ Регистрация врача, визитка с геолокацией и фото
- ✅ Календарь и запись пациентов (текстовый ввод «ФИО — услуга»)
- ✅ Просмотр расписания на день и месяц
- ✅ Просмотр прайс-листа (без редактирования)

### Standard (tier=1) — Стандартный
- ✅ Всё из Basic
- ✅ База пациентов: добавление, поиск по ФИО и телефону
- ✅ Запись на приём через выбор пациента из базы или создание нового
- ✅ История болезни по каждому пациенту (услуги, комментарии)
- ✅ Управление визитами: удаление и **перенос записи** на другую дату/время
- ✅ Имплантологическая карта и генерация PDF-карты

### Premium (tier=2) — Премиум
- ✅ Всё из Standard
- ✅ Финансовый учёт: внесение оплат, скидки на услугу и на всю работу
- ✅ Прайс-лист с редактированием (категории, цены, длительность)
- ✅ Генерация PDF-счетов (услуги, скидки, оплачено, долг)
- ✅ Экспорт базы в Excel (пациенты, записи, история, импланты)

---

## Возможные проблемы и решения

### 1. Ошибка подключения к БД
**Проблема:** `asyncpg.exceptions.InvalidPasswordError`

**Решение:**
- Проверьте правильность пароля в `DATABASE_URL`
- Убедитесь, что PostgreSQL запущен
- Проверьте, что база данных `ministom` существует

### 2. Команда `alembic` не найдена
**Проблема:** `alembic: command not found`

**Решение:**
- Убедитесь, что виртуальное окружение активировано
- Переустановите зависимости: `pip install -r requirements.txt`

### 3. Ошибка импорта модулей
**Проблема:** `ModuleNotFoundError`

**Решение:**
- Убедитесь, что вы запускаете из корневой директории проекта
- Используйте `python -m app.main` вместо `python app/main.py`

### 4. WeasyPrint не работает
**Проблема:** Ошибки при генерации PDF

**Решение:**
- На Windows может потребоваться установка GTK+
- На Linux: `sudo apt-get install libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info`
- На Mac: `brew install cairo pango gdk-pixbuf libffi`

### 5. Бот не отвечает
**Проблема:** Бот запущен, но не реагирует на команды

**Решение:**
- Проверьте правильность `BOT_TOKEN` в `.env`
- Убедитесь, что бот запущен (нет ошибок в консоли)
- Проверьте логи на наличие ошибок

---

## Дополнительная информация

### Технологии
- **Python 3.10+**
- **aiogram 3.x** - асинхронный фреймворк для Telegram ботов
- **PostgreSQL** - база данных
- **SQLAlchemy (Async)** - ORM
- **Alembic** - миграции БД
- **WeasyPrint** - генерация PDF
- **Jinja2** - шаблоны HTML

---

## Как пользоваться админ-панелью

Доступ только у пользователей, чей **Telegram ID** указан в переменной **ADMIN_IDS** в `.env`.

### Настройка

В файле `.env` добавьте (через запятую, без пробелов):

```env
ADMIN_IDS=123456789,987654321
```

Узнать свой Telegram ID можно у бота [@userinfobot](https://t.me/userinfobot).

### Команды в боте

| Команда | Описание |
|--------|----------|
| `/admin` | Открыть меню админа: справка и кнопка «Админка (Web App)» |
| `/admin_list_users` | Список до 50 пользователей: имя, Telegram ID, уровень, срок подписки |
| `/admin_set_tier <telegram_id> <tier>` | Поставить уровень без срока. tier: 0=Basic, 1=Standard, 2=Premium |
| `/admin_set_subscription <telegram_id> <tier> <days>` | Уровень + подписка на N дней (считается дата окончания) |

**Примеры:**

- Выдать пользователю с ID 555666 Standard на 30 дней:  
  `/admin_set_subscription 555666 1 30`
- Поставить Premium без срока:  
  `/admin_set_tier 555666 2`
- Посмотреть всех пользователей:  
  `/admin_list_users`

В списке пользователей в скобках указан **telegram_id** — его и подставляйте в команды вместо `<user_id>`.

### Web App (веб-админка)

Если запущена веб-админка (см. папку `admin_webapp/` и `admin_webapp/README.md`):

- В боте по команде `/admin` появляется кнопка **«Админка (Web App)»**.
- В браузере открывается страница, где можно:
  - видеть список пользователей подписки;
  - смотреть уровни (Basic / Standard / Premium) и сроки подписки;
  - вручную менять уровень и дату окончания подписки.

Запуск и настройка Web App описаны в **admin_webapp/README.md**.

---

### Безопасность
- Никогда не коммитьте файл `.env` в репозиторий
- Храните `BOT_TOKEN` в секрете
- Используйте сильные пароли для БД
- Ограничьте доступ к админ-командам (только ADMIN_IDS)

### Производительность
- Все операции с БД асинхронные
- PDF генерируется в отдельном потоке (не блокирует бота)
- Middleware кэширует данные пользователя

---

## Чек-лист перед продакшеном

Перед выкладкой на сервер убедитесь:

| Проверка | Действие |
|----------|----------|
| **Переменные окружения** | На сервере задайте `BOT_TOKEN`, `DATABASE_URL`, `ADMIN_IDS` (через systemd `Environment=` или в `.env`). **Не коммитьте `.env` с реальными данными** — используйте `.env.example` как шаблон. |
| **База данных** | PostgreSQL установлен, создана БД `ministom`, применены миграции: `alembic upgrade head`. |
| **Зависимости для PDF** | На Linux установлены пакеты для WeasyPrint: `libcairo2`, `libpango-1.0-0`, `libpangocairo-1.0-0`, `libgdk-pixbuf2.0-0`, `libffi-dev`, `shared-mime-info`. |
| **Автозапуск и перезапуск** | Бот запущен через systemd (или аналог) с `Restart=always`, `RestartSec=10`, чтобы при падении процесс поднимался. |
| **Логи** | Логи пишутся в stdout/stderr и при необходимости перенаправляются в journald или файл для разбора ошибок. |
| **Обработка ошибок** | В проекте включён глобальный обработчик ошибок: при сбое в хендлере пользователь получит сообщение «Временная ошибка», бот не упадёт. |
| **Пул БД** | Включены `pool_pre_ping` и `pool_recycle` — соединения с БД перепроверяются и обновляются при долгой работе. |

После выполнения пунктов выше бот готов к стабильной работе без лишних падений.

---

## Размещение на сервере (работа 24/7)

Чтобы бот работал постоянно, его нужно запустить на сервере или в облачном сервисе. Ниже — варианты с учётом требований MiniStom.

### Что нужно боту

- **Python 3.10+** — основной процесс (aiogram, polling).
- **PostgreSQL** — база данных (обязательно).
- **Системные библиотеки для WeasyPrint** (генерация PDF): на Linux — `libcairo2`, `libpango`, `libgdk-pixbuf2.0-0`, `libffi-dev` и т.п. (см. документацию WeasyPrint).
- **Переменные окружения:** `BOT_TOKEN`, `DATABASE_URL`, `ADMIN_IDS`; по желанию `ADMIN_WEBAPP_URL`, `TIMEZONE_API_KEY`.
- Для **веб-админки** — HTTPS (обязательно для Telegram Web App).

### Сравнение вариантов

| Вариант | Плюсы | Минусы | Ориентир цены |
|--------|--------|--------|----------------|
| **VPS (свой сервер)** | Полный контроль, WeasyPrint без проблем, один сервер и бот, и админка | Нужно самому ставить ОС, PostgreSQL, зависимости | от ~300–500 ₽/мес |
| **Railway** | Деплой из GitHub, встроенный PostgreSQL, не нужно администрировать ОС | WeasyPrint лучше запускать через Docker (образ с cairo/pango) | бесплатный tier, потом от $5 |
| **Render** | Похоже на Railway, есть PostgreSQL | То же про WeasyPrint — обычно Docker | бесплатный tier, потом от $7 |
| **Fly.io** | Удобен для Docker, есть регионы | Нужен Dockerfile и настройка | от бесплатного tier |
| **Timeweb Cloud / Selectel / и т.п.** | Российские дата-центры, оплата в рублях | По сути VPS — настраивать всё самому | от ~200–400 ₽/мес |

Рекомендация: для максимальной простоты и гарантированной работы WeasyPrint — **VPS**. Для деплоя «из репозитория одним кликом» — **Railway** или **Render** с Docker.

---

### Вариант 1: VPS (универсально, 24/7)

Подойдёт любой VPS с Linux (Ubuntu 22.04, Debian 12, и т.д.): **Timeweb**, **Selectel**, **Reg.ru**, **Hetzner**, **DigitalOcean**, **Vultr** и др.

1. **Создайте сервер** (Ubuntu 22.04 LTS), зайдите по SSH.

2. **Установите PostgreSQL и зависимости для WeasyPrint:**
   ```bash
   sudo apt update
   sudo apt install -y postgresql postgresql-contrib python3.11 python3.11-venv python3-pip
   sudo apt install -y libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info
   ```

3. **Создайте БД и пользователя:**
   ```bash
   sudo -u postgres psql -c "CREATE USER ministom WITH PASSWORD 'ваш_пароль';"
   sudo -u postgres psql -c "CREATE DATABASE ministom OWNER ministom;"
   ```

4. **Склонируйте проект, настройте .env:**
   ```bash
   cd /opt
   sudo git clone https://github.com/ваш-репо/MiniStom.git
   cd MiniStom
   sudo python3.11 -m venv venv
   sudo ./venv/bin/pip install -r requirements.txt
   sudo cp .env.example .env
   sudo nano .env   # BOT_TOKEN, DATABASE_URL=postgresql+asyncpg://ministom:пароль@localhost:5432/ministom, ADMIN_IDS
   ```

5. **Миграции и запуск:**
   ```bash
   sudo ./venv/bin/alembic upgrade head
   sudo ./venv/bin/python -m app.main
   ```

6. **Чтобы бот работал 24/7 после отключения SSH**, запускайте через **systemd** (создайте файл `/etc/systemd/system/ministom.service`):
   ```ini
   [Unit]
   Description=MiniStom Telegram Bot
   After=network.target postgresql.service

   [Service]
   Type=simple
   User=root
   WorkingDirectory=/opt/MiniStom
   Environment="PATH=/opt/MiniStom/venv/bin"
   ExecStart=/opt/MiniStom/venv/bin/python -m app.main
   Restart=always
   RestartSec=10

   [Install]
   WantedBy=multi-user.target
   ```
   Затем: `sudo systemctl daemon-reload`, `sudo systemctl enable ministom`, `sudo systemctl start ministom`, `sudo systemctl status ministom`.

Веб-админку на том же сервере можно запустить через отдельный unit (uvicorn) и выдать HTTPS через nginx + Let's Encrypt, затем в `.env` бота указать `ADMIN_WEBAPP_URL=https://ваш-домен`.

---

### Вариант 2: Railway

В корне проекта есть **Dockerfile** и **.dockerignore** — Railway соберёт образ с WeasyPrint (Cairo/Pango).

1. Зарегистрируйтесь на [railway.app](https://railway.app), создайте проект.
2. Добавьте **PostgreSQL**: в проекте нажмите **Add Service** → **Database** → **PostgreSQL**. В настройках PostgreSQL скопируйте переменную `DATABASE_URL` (формат `postgresql://...`). Для приложения замените схему на **`postgresql+asyncpg://`** (в начале URL).
3. Добавьте сервис из репозитория: **Add Service** → **GitHub Repo** → выберите **DentShare/Mystom** (или ваш репо). Railway определит Dockerfile и соберёт образ.
4. В настройках **сервиса бота** (не БД) откройте **Variables** и задайте:
   - `BOT_TOKEN` — токен от @BotFather
   - `ADMIN_IDS` — один или несколько Telegram ID через запятую (например `123456789`)
   - `DATABASE_URL` — скопируйте из сервиса PostgreSQL и замените в начале `postgresql://` на `postgresql+asyncpg://`
   - По желанию: `ADMIN_WEBAPP_URL`, `TIMEZONE_API_KEY`
5. **Сборка**: в разделе **Settings** сервиса убедитесь, что используется **Dockerfile** (Root Directory — пусто, Dockerfile path — `Dockerfile`). При пуше в GitHub деплой запустится автоматически.
6. Сервис бота не открывает HTTP-порт — это нормально. Оставьте тип сервиса как есть; бот работает по long polling. Деплой — при пуше в GitHub; сервис по умолчанию «всегда включён».

Админку (FastAPI) можно развернуть вторым сервисом в том же проекте и задать для бота `ADMIN_WEBAPP_URL` на URL этого сервиса (Railway даёт HTTPS).

---

### Вариант 3: Render

Аналогично Railway: [render.com](https://render.com) → New → Web Service (или Background Worker для бота) + PostgreSQL. Добавьте переменные окружения, при необходимости используйте Dockerfile с зависимостями для WeasyPrint. Для 24/7 выберите платный план (free tier может «засыпать»).

---

### Кратко

- **Нужна максимальная предсказуемость и минимум возни с PDF** → VPS (Timeweb, Selectel, Hetzner и т.д.) + systemd.
- **Хочется деплой из GitHub без своего сервера** → Railway или Render с Docker и встроенным PostgreSQL.

После настройки любой из вариантов бот может работать **24/7** с учётом всех требований (Python, PostgreSQL, WeasyPrint, при необходимости веб-админка по HTTPS).

---

## Поддержка

При возникновении проблем:
1. Проверьте логи бота в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность конфигурации в `.env`
4. Убедитесь, что PostgreSQL запущен и доступен

---

**Удачной работы с MiniStom Bot! 🦷**

